/* ============================================================
   This just creates the shared data object.
   Each country's own folder (e.g. France/data.js) adds its
   entries into it. You shouldn't need to edit this file.
   ============================================================ */
const data = {};
